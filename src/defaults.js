$.paprless.defaults = {
   // add your default settings for your component here.
   // these will be overridden by whatever options the user
   // provides when they initialize your component like:
   // $('#someselector').paprless({ something: 'their-value' });
   something: false
};

